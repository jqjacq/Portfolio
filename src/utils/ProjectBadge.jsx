const ProjectBadge = ({ children }) => {
  return (
    <span className="!font-bold inline-block bg-purple-100 text-white text-xs font-semibold px-3 py-1 m-1 rounded-full">
      {children}
    </span>
  );
};

export default ProjectBadge;
